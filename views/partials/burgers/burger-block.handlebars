<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-newstatus="{{devoured}}">
		{{#if devoured}}FRESH FROM THE GRILL!{{else}}THAT WAS YUMMY!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
